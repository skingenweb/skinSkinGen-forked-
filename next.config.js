/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com'],
    },
    videos:{
      domains: ['res.cloudinary.com'],
    }
  };
  
  module.exports = nextConfig;
  